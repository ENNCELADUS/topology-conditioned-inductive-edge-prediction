{
  "registration_id": "g5-stage1-20260716-membership-normalized-screen-v2",
  "gate": "G5 Stage 1 - EgoStitch minimum-viable model (imagination + degree budget + closure)",
  "created_utc": "2026-07-16T00:00:00Z",
  "amendment_rationale": "Post-diagnostic repair registration. The retained Seed-0 artifact showed a pair-invariant residual caused first by s1 membership-kernel scale collapse, with the s0-saturated training distribution providing no recovery pressure; loss-family competition was not measured because the registered gradient instrumentation was absent. This revision L2-normalizes only the s1 kernel operands, implements the pre-existing gradient-norm/Kendall fallback contract, registers channel/rank-mobility fidelity series and a conjunctive dead-residual validity gate, and adds a validation-AUPRC near-tie fidelity tie-break. It does not retroactively bind the completed diagnostic. The sampler, comparators, topology metrics, exact-quota operating point, 0.005 RD tolerance, guards, and single-seed engineering-screen success rule are unchanged; hard-negative exposure remains contingent on a later registration only if this screen has alive channels but no topology movement.",
  "spec_binding": "docs/05-egostitch-spec.md §13",
  "protocol_binding": "docs/03-experiment-protocol.md §5.0.5, §5.2",
  "benchmark": {
    "alias": "Benchmark-A",
    "strategy": "breadth_first"
  },
  "seeds": [0],
  "frozen_inputs": {
    "b0_candidate_scores": {
      "path": "outputs/deliverables/b0_v31_breadth_first_20260711/scores/candidate.npz",
      "sha256": "c5873caa3fece651d1155fd725e04f26a432ceee59d8e78dcda5f4acd687a95d",
      "checkpoint_id": "e092537d8cf1e208"
    },
    "g1_results": {
      "path": "outputs/deliverables/g1_graph_metrics_20260714/g1_results.json",
      "sha256": "668129a7c300d87682250382a36ef854c1f301c21d078b261f2b0fe2ae9d1ca4"
    },
    "g3_results": {
      "path": "outputs/deliverables/g3_graph_metrics_20260714/g3_results.json",
      "sha256": "e7fbc8e4e7f76ffe1f50123ac8e96c776cded79406e01e638539a3f835b79c24"
    }
  },
  "comparators": [
    "b0",
    "b0_cal_density",
    "b0_cal_selfdensity",
    "b0_cal_degseq"
  ],
  "comparator_note": "B1/B5 comparison rows are deferred to E3 with their implementations (protocol §5.0.5 instantiation, 2026-07-14). The Stage-1 bar on every axis is the best comparator on that axis (max over B0 and all B0+cal arms). All comparator rows are recomputed by the gate module from the frozen candidate-scores artifact and the committed b0_cal results; values are never copied from result notes.",
  "operating_point": {
    "assembly": "density-matched threshold on non-self candidate rows against target_edges = |E(strip_self_loops(test_graph))| with atomic tie handling (G1 convention); self-pairs assembled at the same threshold",
    "matched_global_rd": "for each comparator, select exactly its realized non-self edge count from EgoStitch candidate rows by descending pass-1 score; split only the boundary score tie by ascending canonical pair order (min node index, max node index), without labels or topology targets; assemble self-pairs whose score is at least the selected boundary score; require and disclose |RD_global(egostitch) - RD_global(comparator)| <= 0.005 (the exact-quota construction gives zero gap for a valid quota)",
    "scores": "Stage-1 scores are pass-1 (ratio-1) scores per spec §13.11; EgoStitch artifacts must declare the §13.16 pair-pass-fp32 precision contract and record descriptive score-resolution diagnostics"
  },
  "fidelity_validity_gate": {
    "rule": "Before topology metrics, fail as a dead residual only if all three registered conditions hold after exact pair alignment to frozen s0; this is run validity, not a success criterion.",
    "min_residual_std_ratio": 1e-5,
    "max_spearman": 0.9999,
    "max_topk_overlap": 0.9999,
    "topk_fraction": 0.01
  },
  "s0_candidate_contract": {
    "checkpoint_id": "e092537d8cf1e208",
    "pairs_source": "candidate",
    "strategy": "breadth_first",
    "precision": "fresh fp32 score-universe artifact from the audited frozen B0 checkpoint; it is supplied separately from the historical canonical comparator artifact and must have exact pair-universe alignment with the EgoStitch candidate artifact"
  },
  "primary_criteria": {
    "family": ["clustering_mmd_ratio", "bfs_macro_gs", "bfs_macro_rd"],
    "decision_procedure": "single_seed_point_estimate_dominance",
    "rationale": "Headroom-weighted from the completed G3 gate: Oracle-blend clustering-MMD headroom 3.885 is the largest measured axis; Oracle-topo shows GS/local-RD headroom (1.612x / 0.794 vs 0.422). Pure topology signal can regress the degree axis (Oracle-topo degree headroom 0.926), so degree is a guard, not a primary criterion.",
    "criteria": [
      {
        "metric": "clustering_mmd_ratio",
        "direction": "lower_is_better",
        "rule": "Seed-0 point estimate strictly lower than every comparator at the canonical operating point"
      },
      {
        "metric": "bfs_macro_gs",
        "direction": "higher_is_better",
        "rule": "Seed-0 point estimate strictly higher than every comparator at matched global simple-edge RD"
      },
      {
        "metric": "bfs_macro_rd",
        "direction": "higher_is_better",
        "rule": "Seed-0 point estimate strictly higher than every comparator at matched global simple-edge RD"
      }
    ],
    "statistics_procedure": "No inferential acceptance procedure is valid with one training seed. The gate records point estimates and descriptive bootstrap uncertainty where available, but p-values, confidence-interval pass flags, and Holm decisions are not applicable and must not be used for acceptance. All three deterministic dominance criteria must pass.",
    "all_must_pass": true
  },
  "guards": [
    {
      "name": "degree_mmd_non_regression",
      "rule": "Seed-0 degree-MMD ratio <= 1.10 x the B0 degree-MMD ratio recomputed from the frozen candidate artifact at the G1 operating point"
    },
    {
      "name": "matched_edge_auprc",
      "rule": "Seed-0 AUPRC on the degree-corrected ratio-1 regime >= (B0's recomputed value on the same regime) - 0.02; hard-heuristic and hard-feature regime rows are disclosed but not gated at Stage 1"
    }
  ],
  "diagnostics_nonbinding": [
    "degree-calibration curve: E[d_hat * rho_hat_eval / rho_train] vs realized assembled degree (spec §4.6 transmission evidence)",
    "slot recall@K on train-side G_struct (primary) and disclosed test-side row",
    "slot-adjacency vs true local clustering correlation (Pearson/Spearman)",
    "s-channel correlation matrix over (s0, s1, s2) on validation queries",
    "self/non-self edge-metric split and self-loop-rate row (spec §9.4.3)",
    "proj(x) batch variance trajectory (representation-collapse watch, spec §13.7)",
    "per-epoch s1/s2/s2_aa scale, residual scale, residual/s0 ratio, Kendall rank mobility, and fixed top-1% overlap",
    "weighted per-family gradient-norm series every 50 steps plus Kendall trigger/activation state",
    "FLOPs and wall-clock table: per-node cached, per-pair marginal, full candidate-universe assembly (proposal §4.7 R=0 commitment)"
  ],
  "verdict_rule": "single-seed screening pass iff Seed 0 strictly dominates every comparator on all three primary criteria AND both guards hold. P-values, confidence-interval pass flags, and Holm decisions are not applicable. Otherwise the screening verdict is 'cut' and the registered failure reading below is written into the gate results verbatim.",
  "failure_reading": "If Stage 1 fails the primary family against B0+cal, the generative imagination apparatus is not load-bearing at this stage (§5.2.1 wording): the calibrated-assembly story is written up honestly and the locked-decision discussion is taken before Stage 2. If Stage 1 fails only against B0 with B0+cal also failing against B0, the assembly-calibration axis is re-examined before mechanisms are cut.",
  "decision_rules_5_2_verbatim": [
    "For the G5 Stage-1 engineering screen, if the fixed Seed-0 run does not strictly dominate every registered comparator on all three primary topology axes while passing both guards, the Stage-1 mechanism is cut; no statistical-significance or cross-seed-robustness claim is made.",
    "If s1/s2 knockouts cost nothing on every benchmark and stratum, `Ours -> B5` is declared and the honest small-paper outcome (\"calibrated block-model marginals close most of the pair-to-topology gap\") is written.",
    "For E1/E3 paper-level comparisons, Holm over the held-out assembled family and at least three seeds remain required; the one-seed Stage-1 screen does not replace that rule.",
    "All thresholds and rules recorded in run metadata before E1/E3 held-out metrics are opened."
  ],
  "mechanics": {
    "worker_obligation": "src/train_egostitch.py records this file's sha256 as run_metadata.json['preregistration_sha256'] at run start, before the first optimizer step.",
    "gate_obligation": "src/experiments/g5_stage1.py recomputes this file's sha256 and refuses to open held-out metrics unless it equals the value recorded in every supplied run's run_metadata.json. The dead-residual check consumes a separately supplied fresh-fp32 s0 candidate artifact from the registered B0 checkpoint, not the historical quantized comparator deliverable.",
    "hyperparameters": "spec-default (docs/05-egostitch-spec.md §0/§13) x fixed Seed 0; additional seeds are optional robustness analyses. The 30-config HPO-parity and at-least-three-seed Holm requirements apply at E1/E3, not at this Stage-1 screening gate.",
    "training_diagnostics": "Fixed probe batch after warm-start; weighted L_edge/L_recon/L_real/L_ssl global L2 gradient norms every 50 steps; activate pre-instantiated Kendall weights only after >10x median persists for 1000 steps. Probe abort |mean(s1)| > 1000. Validation AUPRC is primary checkpoint selection; inside absolute tolerance 1e-4, larger residual/s0 std ratio breaks the tie.",
    "p4_exclusion": "No hard heuristic/feature negatives and no hard-negative checkpoint criterion are added in this revision. Such exposure requires a later spec/registration amendment only if channels are alive but topology remains unchanged."
  }
}
