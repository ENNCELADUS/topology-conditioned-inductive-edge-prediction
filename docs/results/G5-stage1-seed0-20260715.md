# G5 Stage-1 Formal Training: Seed 0

## Result boundary

**Status:** Seed 0 training completed successfully on 2026-07-15. The formal G5
Stage-1 experiment is **not complete**: Seed 1 stopped at the artifact performance
gate, Seed 2 was not run, and no held-out candidate-universe or assembled-graph gate
has been evaluated. This note records a model-training result, not a G5 pass/fail
decision.

**Protocol note (2026-07-16):** after a later exact-quota Seed-0 diagnostic was
inspected, Stage 1 was re-scoped to a one-fixed-seed engineering screening gate. This
historical artifact is not retroactively rebound: its recorded registration hash is
superseded, and a binding screening run must start under the replacement experiment ID
and supply the required fidelity/cost reports. E1/E3 still require multi-seed Holm
inference.

## Run identity

| Field | Value |
|---|---|
| Git commit | `31afbe6` |
| Seed / partition seed | `0 / 0` |
| Config hash | `f1960c602923a24b1900d93532fc59e6d5d94fbf8f81a13ce36bc1f4d6811178` |
| Pre-registration SHA-256 | `26b76ea83a4d838d8c2eaaec949695b14bbbb618d7320977de241038c8578ce4` |
| Frozen S0 checkpoint ID | `e092537d8cf1e208` |
| EgoStitch checkpoint ID | `54f3c0ad8f5dfc18` |
| Hardware | 2 × NVIDIA H20 |
| DDP world size | `2` |
| PyTorch | `2.10.0+cu128` |
| Positive-edge mode | `e_sup` |

## Validation result

The selected checkpoint is epoch 14, chosen by validation AUPRC as pinned in spec
§13.8.

| Metric | Value |
|---|---:|
| AUROC | `0.945766` |
| AUPRC | `0.951966` |
| Accuracy | `0.850438` |
| Precision | `0.959810` |
| Recall / sensitivity | `0.732435` |
| Specificity | `0.969147` |
| F1 | `0.830847` |
| MCC | `0.721724` |
| ECE | `0.137748` |
| Brier score | `0.126705` |

The validation curve is nearly flat: AUPRC ranges from `0.951582` to `0.951966`
over 30 epochs. Epoch 1 is `0.951584`; the best absolute increase is `0.000382`.
Therefore this training artifact alone does not establish that the learned Stage-1
channels improve on frozen S0. That comparison belongs to the held-out G5 gate.

## Runtime and artifact validation

| Field | Value |
|---|---:|
| Total pipeline time | `807.389 s` |
| Train-stage time | `673.230 s` |
| Epochs / validations | `30 / 30` |
| Global training pairs | `1,543,680` |
| Global throughput | `2,346.349 pairs/s` |
| Selected token budget per rank | `128` |
| Peak memory per rank | `2.041 / 2.041 GiB` |
| Steady-state data-wait fraction | `0.0495125` |
| Feature-cache hit rate | `1.0` |
| Exact train/validation coverage | `true / true` |
| Counterfactual stop epoch | `24` |

`best.pt`, `last.pt`, `metrics.jsonl`, `run_metadata.json`, and `profile.json` match
their published SHA-256/byte-size manifest. The retained local artifacts are under
`outputs/egostitch_stage1/seed0/` and are intentionally gitignored.

## Incomplete formal run

The same formal sequence started Seed 1, which stopped before atomic publication with:

```text
worker profile is missing or malformed:
steady_state_data_wait_fraction exceeds 0.05
```

No Seed-1 scientific metrics are retained or reported, and Seed 2 did not start. Under
the superseded registration, the next valid result would have required three complete
seeds. Under the current replacement contract, a valid Stage-1 screening result instead
requires one newly bound fixed-Seed-0 run plus candidate scoring, fidelity/cost
diagnostics, B0/`B0+cal` comparators, and the registered assembled-graph decision rule.
