"""S2 — set-conditioned joint latent topology generation diagnostic.

Given only intrinsic features ``X_S`` of an unseen protein set, tests whether a
conditional generative model ``p(Z_{1:n} | X_S)`` produces node-aligned joint
latent topology. Exploratory diagnostic: ``evidence_class=diagnostic``,
``formal:false``. Spec: ``docs/tmp/s2_set_generation_experiment.md``.
"""
